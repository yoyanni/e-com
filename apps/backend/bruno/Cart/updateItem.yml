info:
  name: updateItem
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/cart/{{cartItemId}}"
  body:
    type: json
    data: |-
      {
        "quantity": "2"
      }
  auth: inherit

runtime:
  variables:
    - name: cartItemId
      value: 8507d7af-b434-4bcb-a8cc-3d0debe38928

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
