info:
  name: findOne
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/products/{{slug}}"

runtime:
  variables:
    - name: slug
      value: handmade-rubber-chair

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
